import { RequestArticleModal, REQUEST_ARTICLE_MODAL } from "./RequestArticleModal";

export const WineryModals = () => {
    return (
        <>
            <RequestArticleModal id={REQUEST_ARTICLE_MODAL} />
        </>
    );
};
