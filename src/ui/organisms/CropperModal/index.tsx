import { ImageCropperModal, IMAGE_CROPPER_MODAL } from "./ImageCropperModal";

export const CropperModals = () => {
    return (
        <>
            <ImageCropperModal id={IMAGE_CROPPER_MODAL} />
        </>
    );
};
